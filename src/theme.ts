import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#00ABD0',
            contrastText: '#FFFFFF'
        },
        secondary: {
            main: '#162953',
            contrastText: '#FFFFFF'
        },
    },
});